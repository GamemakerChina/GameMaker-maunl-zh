{
    "Command-line parameters are those extra commands that you can add to an exe to change how the program is run and with this function you can get the chosen command-line parameter as a string. You can find the number of parameters for the current game using the function {}{}parameter_count(){}{}, where the first parameter has index 1 and the last one has the index returned by the function (a value of 0 is special on that it is the filename of the game executable, including the path). It should be noted that this function will work for on the HTML5 platform, retrieving the url parameters.":"命令行参数是您可以添加到exe以更改程序运行方式的额外命令，并且使用此函数您可以将所选命令行参数作为字符串获取。您可以使用函数查找当前游戏的参数数量{}{}parameter_count(){}{}其中第一个参数的索引为1，最后一个参数的索引由函数返回(值0是特殊的，因为它是游戏可执行文件的文件名，包括路径)。需要注意的是，此函数适用于HTML5平台，检索url参数。",
    "The above code will get the number of command-line parameters, and if there is 1 or more it will loop through them and store them as strings in an array.":"上面的代码将获取命令行参数的数量，如果有1个或更多，它将遍历它们并将它们存储为数组中的字符串。"
}