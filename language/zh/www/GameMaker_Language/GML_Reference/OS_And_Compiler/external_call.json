{
    "If you have created an external function call to a {}dll{} or {}dylib{} using {}{}external_define(){}{}, you can use this function to then call it. You supply the name of the previously defined function as well as each of the arguments it requires (each argument must be of the correct type, either real or string) and the function returns the result of the external call.":"如果使用了创建调用外部{}dll{}或{}dylib{}的函数{}{}external_define(){}{}, 可以使用此函数然后调用它。您提供先前定义的函数的名称以及它需要的每个参数(每个参数必须是正确的类型，实数或字符串)，并且该函数返回外部调用的结果。",
    "The above example code calls a previously defined external function and stores the returned value in a local variable.":"上面的示例代码调用先前定义的外部函数并将返回值存储在局部变量中。",
    "The different arguments that you want to pass through to the external function":"要传递给外部函数的不同参数",
    "The name of the function that you want to call":"要调用的函数的名称",
    "{}{}Any{}{} (the type of value returned will depend on the defined function)":"{}{}Any{}{}(返回值的类型将取决于定义的函数)"
}