{
    "An instance can be flagged as {}visible{} or not by setting this variable to {}true{} (visible) or {}false{} (invisible). This works by telling {}GameMaker{} to skip the draw event for this instance, so care must be taken when using this as it means that no code placed in the draw event will be run while the instance is not visible. Also note that if the {}layer{}&nbsp;that the instance is assigned to is flagged as invisible, setting this variable to {}true{} will have no effect until the layer itself is flagged as visible too.":"通过将此变量设置为 {}true{}(可见) 或 {}false{}(不可见)，可以将实例标记为 {}visible{} 或不标记。这是通过告诉 {}GameMaker{} 跳过此实例的绘制事件来实现的，因此在使用此方法时必须小心，因为这意味着当实例不可见时，将不会运行放置在绘制事件中的代码。另请注意，如果将实例分配到的{}层{} 标记为不可见，则将此变量设置为 {}true{} 将不起作用，直到层本身也标记为可见。",
    "An instance can be flagged as {}visible{}&nbsp;or not by setting this variable to {}true{}&nbsp;(visible) or {}false{}&nbsp;(invisible). This works by telling {}GameMaker{} to skip the draw event for this instance, so care must be taken when using this as it means that no code placed in the draw event will be run while the instance is not visible. Also note that if the {}layer{}&nbsp;that the instance is assigned to is flagged as invisible, setting this variable to {}true{}&nbsp;will have no effect until the layer itself is flagged as visible too.":"通过将此变量设置为 {}true{}( 可见) 或 {}false{}( 不可见)，可以将实例标记为 {} 可见 {} 或不标记。其工作原理是告诉 {}GameMaker{} 跳过此实例的绘制事件，因此使用此功能时必须小心，因为这意味着当实例不可见时，不会运行放置在绘制事件中的任何代码。另请注意，如果实例分配到的 {} 层 {} 被标记为不可见，则将此变量设置为 {}true{} 将不起作用，直到该层本身也被标记为可见。",
    "The above code will check the visible flag of the \"other\" instance in a {}collision event{} and if it is set to {}true{} move that instance back to its previous position.":"上面的代码将检查{}碰撞事件{}中\"other\"实例的可见标志，如果它被设置为{}true{}，则将该实例移回其先前的位置。",
    "The above code will check the {}visible{} flag of the \"other\" instance in a {}collision event{} and if it is set to {}true{}&nbsp;move that instance back to its previous position.":"上述代码将检查{2}碰撞事件{3}中\"其他\"实例的 {}visible{} 标志，如果设置为{}true{}，则将该实例移回其先前位置。"
}