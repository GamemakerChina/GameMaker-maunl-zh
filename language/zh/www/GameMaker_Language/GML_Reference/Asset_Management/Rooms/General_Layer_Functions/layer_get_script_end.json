{
    "The above code will check to see if the layer that the instance running the code has a script function assigned to it and if it doesn\u0027t one is assigned.":"上面的代码将检查运行代码的实例是否具有分配给它的脚本函数的层，以及是否分配了一个脚本函数。",
    "The unique ID value of the layer to target (or the layer name as a string)":"目标层的唯一ID值(或作为字符串的层名)",
    "This function returns the {}script function{} index of the function assigned to run at the end of rendering the given layer, or it will return -1 if no function is assigned. You supply the layer ID (which you get when you create the layer using {}{}layer_create(){}{}) or the layer name (as a string - this will have a performance impact). You can assign script functions to a layer with&nbsp;{}{}layer_script_begin(){}{}&nbsp;and {}{}layer_script_end(){}{}.":"此函数返回指定在渲染给定层结束时运行的函数的{}脚本函数{}索引，如果未分配任何函数，则返回-1。提供层 ID(使用{}{}layer_create(){}{}创建层时获得的 ID)或层名称(作为字符串，这将影响性能)。您可以使用{}{}layer_script_begin(){}{}和{}{}layer_script_end(){}{}指定脚本函数给层。"
}