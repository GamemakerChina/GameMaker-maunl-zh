{
    "The above code checks the given layer horizontal and vertical speeds and if they are not both set to 0 then it is sets them to 0.":"上述代码检查给定层水平和垂直速度，如果它们没有都设置为0，则将其设置为0。",
    "The unique ID value of the layer to set the vertical speed of":"要设置垂直速度的层的唯一ID值",
    "The vertical speed (in pixels per game frame) to set":"要设置的垂直速度(每游戏帧像素)",
    "You can use this function to set the vertical speed (in pixels per game frame) of the layer within the currently scoped room. You supply the layer ID (which you get when you create the layer using {}{}layer_create(){}{}) or the layer name (as a string - this will have a performance impact) and the speed value to set, where a positive value is downwards and a negative value upwards.":"您可以使用此函数设置当前范围房间内图层的垂直速度(每游戏帧的像素数)。提供层 ID(使用{}{}layer_create(){}{}创建层时获得的 ID)或层名称(作为字符串，这将对性能产生影响)和要设置的速度值，其中正值向下，负值向上。"
}