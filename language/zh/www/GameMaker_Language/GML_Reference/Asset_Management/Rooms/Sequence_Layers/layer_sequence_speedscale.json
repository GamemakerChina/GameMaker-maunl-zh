{
    "The above code creates a new sequence on the layer of the calling instance and then sets its speed scale value to 0.75 (three-quarters default playback speed).":"上述代码在调用实例的层上创建一个新序列，然后将其速度标度值设置为0.75(默认的四分之三回放速度)。",
    "The speed scale to use (a multiplier), default is 1":"要使用的速度刻度(乘数)，默认值为1",
    "The unique ID value of the sequence element to target":"序列元素到目标的唯一ID值",
    "With this function you can change the playback speed of the given sequence. You supply the sequence element ID as returned by {}{}layer_sequence_create(){}{} or by one of the {}layer element functions{} and then a speed scale value. This value is a {}multiplier{}, where 1 is the default playback speed and values less than 1 will slow the playback and values larger than 1 will speed it up, eg: a value of 0.5 would be half playback speed, while a value of 2 would be double playback speed.":"使用该函数，您可以更改给定序列的播放速度。您可以提供由{}{}layer_sequence_create(){}{}或{}层元素函数{}之一返回的序列元素 ID，然后提供速度比例值。此值为{}乘数{}，其中 1 为默认回放速度，小于 1 的值将减慢回放速度，大于 1 的值将加快播放速度，例如：0.5 的值表示一半回放速度，而值为 2 的值则为双倍回放速度。"
}