{
    "The above code will loop through each of the bounding boxes associated with the currently assigned sprite and then draw that data as a string to the screen.":"上面的代码将遍历与当前指定的精灵相关联的每个边界框，然后将该数据作为字符串绘制到屏幕上。",
    "This function will return an array of values associated with any given bounding box for the currently assigned skeleton animation sprite. You supply the index number for the bounding box to get the details of (you can retrieve the total number of bounding boxes for the sprite using the function {}{}skeleton_get_num_bounds(){}{}) and the function will return an array with the following elements:":"此函数将返回与当前指定的骨骼动画精灵的任何给定边界框相关联的值数组。您为边界框提供索引号以获取的详细信息(您可以使用函数{}{}skeleton_GET_NUM_BOUNDS(){}{}来检索sprite的边界框总数)，该函数将返回包含以下元素的数组：",
    "[0] - the number of points on the bounding box (a real)":"[0]-边界框上的点数(实数)",
    "[1] - the name of the bounding box (a string)":"[1]-边界框的名称(字符串)",
    "[2] - the x position of the first point":"[2]-第一个点的X位置",
    "[3] - the y position of the first point":"[3]-第一个点的Y位置",
    "[etc...] - further x/y position data up to the number of points on the bounding box":"[等等]-进一步的X/Y位置数据，直到边界框上的点数",
    "{}NOTE{}&nbsp;The bounding box of a Spine sprite is set up in Spine itself, not in {}GameMaker{}.":"{}注意{}Spine 精灵的边界框是在Spine本身中设置的，而不是在{}GameMaker{}中。"
}