{
    "The above code will loop through each of the bounding boxes associated with the currently assigned sprite and then draw that data as a string to the screen.":"上面的代码将遍历与当前指定的精灵相关联的每个边界框，然后将该数据作为字符串绘制到屏幕上。",
    "This function will return the number of bounding boxes associated with the skeleton animation sprite assigned to the instance running the code. This can then be used along with the function {}{}skeleton_get_bounds(){}{} to retrieve data about each of the bounding boxes.":"此函数将返回与分配给运行代码的实例的骨骼动画精灵相关联的边界框的数量。然后可以将其与函数{}{}skeleton_get_bounds(){}{}一起使用，以检索有关每个边界框的数据。",
    "{}NOTE{}&nbsp;The bounding box of a Spine sprite is set up in Spine itself, not in {}GameMaker{}.":"{}注意{}Spine 精灵的边界框是在Spine本身中设置的，而不是在{}GameMaker{}中。"
}