{
["[2] - the x position of the first point"]={new="[2]-第一个点的X位置";old="[2] - the x position of the first point"};
["[0] - the number of points on the bounding box (a real)"]={new="[0]-边界框上的点数(实数)";old="[0] - the number of points on the bounding box (a real)"};
["This function will return an array of values associated with any given bounding box for the currently assigned skeleton animation sprite. You supply the index number for the bounding box to get the details of (you can retrieve the total number of bounding boxes for the sprite using the function {}{}skeleton_get_num_bounds(){}{}) and the function will return an array with the following elements:"]={old="This function will return an array of values associated with any given bounding box for the currently assigned skeleton animation sprite. You supply the index number for the bounding box to get the details of (you can retrieve the total number of bounding boxes for the sprite using the function {}{}skeleton_get_num_bounds(){}{}) and the function will return an array with the following elements:";new="此函数将返回与当前指定的骨骼动画精灵的任何给定边界框相关联的值数组。您为边界框提供索引号以获取的详细信息(您可以使用函数{}{}skeleton_GET_NUM_BOUNDS(){}{}来检索sprite的边界框总数)，该函数将返回包含以下元素的数组："};
["[1] - the name of the bounding box (a string)"]={old="[1] - the name of the bounding box (a string)";new="[1]-边界框的名称(字符串)"};
["[etc...] - further x/y position data up to the number of points on the bounding box"]={new="[等等]-进一步的X/Y位置数据，直到边界框上的点数";old="[etc...] - further x/y position data up to the number of points on the bounding box"};
["[3] - the y position of the first point"]={old="[3] - the y position of the first point";new="[3]-第一个点的Y位置"};
["The above code will loop through each of the bounding boxes associated with the currently assigned sprite and then draw that data as a string to the screen."]={new="上面的代码将遍历与当前指定的sprite相关联的每个边界框，然后将该数据作为字符串绘制到屏幕上。";old="The above code will loop through each of the bounding boxes associated with the currently assigned sprite and then draw that data as a string to the screen."};
["{}NOTE{}&nbsp;The bounding box of a Spine sprite is set up in Spine itself, not in {}GameMaker{}."]={old="{}NOTE{}&nbsp;The bounding box of a Spine sprite is set up in Spine itself, not in {}GameMaker{}.";new="{}注意{}Spine Sprite的边界框是在Spine本身中设置的，而不是在{}GameMaker{}中。"}
}