{
["The sub-image of the sprite to use."]={old="The sub-image of the sprite to use.";new="要使用的sprite的子图像。"};
["The index of the sprite to use."]={new="要使用的sprite的索引。";old="The index of the sprite to use."};
["This function returns a special {}pointer{} for the sprite texture page. This value can then be used in other draw functions, particularly in the {}2D primitive{}&nbsp;functions, as well as the {}Shader{}&nbsp;functions. You can get more information about the returned texture page using the different {}texture_{} functions found {}here{}."]={new="此函数为精灵纹理页面返回一个特殊的{}指针{}。然后，可以在其他绘制函数中使用此值，特别是在{}2D基本体{}函数以及{}着色器{}函数中。您可以使用在{}此处{}找到的不同{}纹理_{}函数来获取有关返回纹理页面的更多信息。";old="This function returns a special {}pointer{} for the sprite texture page. This value can then be used in other draw functions, particularly in the {}2D primitive{}&nbsp;functions, as well as the {}Shader{}&nbsp;functions. You can get more information about the returned texture page using the different {}texture_{} functions found {}here{}."};
["{}NOTE{} On HTML5, this returns a struct instead of a texture pointer, as a pointer cannot be used on that platform. However this does not change the use of the returned value, as its usage in {}texture functions{} still remains the same."]={old="{}NOTE{} On HTML5, this returns a struct instead of a texture pointer, as a pointer cannot be used on that platform. However this does not change the use of the returned value, as its usage in {}texture functions{} still remains the same.";new="{}注意{}在HTML5上，这将返回一个结构而不是纹理指针，因为指针不能在该平台上使用。但是，这不会改变返回值的用法，因为它在{}纹理函数{}中的用法仍然保持不变。"};
['The above code will draw a 4 vertex triangle strip textured with the texture held in the "tex" variable.']={new="上面的代码将绘制一个4顶点三角形条带纹理，纹理保存在“ tex ”变量中。";old='The above code will draw a 4 vertex triangle strip textured with the texture held in the "tex" variable.'}
}