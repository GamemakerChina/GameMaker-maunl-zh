{
    "The above code will place the referenced sprite into texture memory ready for use.":"上面的代码将把引用的sprite放入纹理内存中以备使用。",
    "The sprite index to fetch":"要提取的sprite索引",
    "This function can be used to prefetch (place into texture memory) a texture page with the given sprite. You supply the sprite index as defined when you created the sprite asset, and the texture page it is on will be loaded into memory. Note that the function will return {}-1{} if prefetch is not supported for the chosen resource or the target platform is HTML5, or it will return {}0{} if all worked correctly.":"此函数可用于预取(放入纹理内存)具有给定精灵的纹理页。您提供创建精灵资源时定义的精灵索引，它所在的纹理页面将加载到内存中。请注意，如果所选资源不支持预取或目标平台为HTML5，则函数将返回{}-1{}；如果一切正常，则函数将返回{}0{}。",
    "{}NOTE{}: There is a performance hit as the texture is uploaded to texture memory on most devices, so it\u0027s recommended that you call {}{}sprite_prefetch{}{} on any required graphics at the start of a level to avoid any stalls.":"{}注意{}：在大多数设备上，当纹理上传到纹理内存时，会对性能造成影响，因此建议您调用{}{}Sprite_Prefetch{}{}on.任何需要的图形在一个级别的开始，以避免任何停顿。"
}