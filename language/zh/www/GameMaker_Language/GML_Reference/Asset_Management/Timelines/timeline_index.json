{
    "The above code will assign the instance running the code a time line indexed in the variable \"global.tl\" if that time line exists.":"上面的代码将为运行该代码的实例分配一个在变量\" global.tl \"中索引的时间线(如果该时间线存在)。",
    "This variable holds the index of the timeline currently associated with the instance. You can set this to a particular time line to use that one, or set it to -1 to stop using a time line for the instance (if no time line is defined for the instance, -1 is returned too). Note that this does {}not{} start the time line - for that use the variable {}{}timeline_running{}{}.":"此变量保存当前与实例关联的时间轴的索引。您可以将其设置为特定的时间线以使用该时间线，或者将其设置为-1以停止使用实例的时间线 (如果未为实例定义时间线，则也将返回-1)。请注意，这不会{}开始使用变量{}{}timeline_running{}{}的时间线。"
}