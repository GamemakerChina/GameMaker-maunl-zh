{
    "The above code creates a new time line and stores its index in the variable \"global.tl\".":"上面的代码创建了一个新的时间线，并将其索引存储在变量\" global.tl \"中。",
    "With this function you can add a new (empty) timeline asset into your game. the function returns the index of this new time line which should be stored in a variable for use in all further function calls that involve this new time line. You should also be sure to use the function {}{}timeline_delete(){}{} whenever you no longer wish to use the time line (like at the end of the room) so as to prevent any possible memory leaks that could slow down or crash your game. To add moments to a timeline created in this way, see the function&nbsp;{}{}timeline_moment_add_script(){}{}.":"使用此功能，您可以将新的 (空) 时间轴资产添加到游戏中。该函数返回此新时间线的索引，该索引应存储在变量中，以便在涉及此新时间线的所有其他函数调用中使用。您还应该确保使用函数{}{}timeline_delete(){}{}每当您不再希望使用时间线 (如在房间的尽头)，以防止任何可能的内存泄漏，可能会减慢或崩溃您的游戏。要将时刻添加到以这种方式创建的时间轴中，请参见函数{}{}timeline_moment_add_script(){}{}。"
}