{
["With this function you can retrieve the names of all assets that have been assigned the given tag or tags. You supply either a single tag string or an {}array{}, where each item in the array is a tag string. The function will return an array where each entry is the name (as a string) of the asset with the given tag. If you need the unique index for the asset, then you can use the function {}{}asset_get_index(){}{} along with the returned name. If there are no assets with the given tag(s), or if there is an error (eg: the given tags do not exist), then an empty array will be returned."]={new="使用此功能，您可以检索已分配给给定标签的所有资产的名称。您可以提供单个标签字符串或{}数组{}，其中数组中的每个项目都是一个标签字符串。该函数将返回一个数组，其中每个条目都是具有给定标记的资产的名称 (作为字符串)。如果需要资产的唯一索引，则可以使用函数{}{}asset_get_index(){}{}以及返回的名称。如果没有带有给定标签的资产，或者如果有错误 (例如: 给定标签不存在)，则将返回一个空数组。";old="With this function you can retrieve the names of all assets that have been assigned the given tag or tags. You supply either a single tag string or an {}array{}, where each item in the array is a tag string. The function will return an array where each entry is the name (as a string) of the asset with the given tag. If you need the unique index for the asset, then you can use the function {}{}asset_get_index(){}{} along with the returned name. If there are no assets with the given tag(s), or if there is an error (eg: the given tags do not exist), then an empty array will be returned."};
["A single asset tag string or an array with various asset tags."]={old="A single asset tag string or an array with various asset tags.";new="单个资产标签字符串或具有各种资产标签的数组。"};
['The above code creates a list, then retrieves the names of all the assets with the tag "background". It loops through the array of names returned, checking to see if any of them are sprite assets, and if they are then the unique index value for the asset is added to the list for future use.']={new="上面的代码创建一个列表，然后检索带有标签 “background” 的所有资产的名称。它循环遍历返回的名称数组，检查是否有任何名称是sprite资产，如果它们是资产的唯一索引值，则将其添加到列表中以备将来使用。";old='The above code creates a list, then retrieves the names of all the assets with the tag "background". It loops through the array of names returned, checking to see if any of them are sprite assets, and if they are then the unique index value for the asset is added to the list for future use.'}
}