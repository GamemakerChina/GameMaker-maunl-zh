{
    "A single asset tag string or an array with various asset tags.":"单个资源标签字符串或具有各种资源标签的数组。",
    "If the function succeeds in adding the tag(s) it will return {}true{} otherwise it will return {}false{}.":"如果函数成功添加标记，它将返回{}true{}，否则将返回{}false{}。",
    "The above code will create an array of tags and then add them to the given object.":"上面的代码将创建一个标签数组，然后将它们添加到给定的对象中。",
    "The name of the asset (a string) or its index value (an integer).":"资源的名称 (字符串) 或其索引值 (整数)。",
    "With this function you can add one or more tag strings to any asset from the asset browser. You supply either the asset name (as a string) or its asset index, as well as either a single tag string or an array where each item is a single tag string. If you supply an asset index value, then you will need to supply the optional asset type argument (a constant), as assets of different types can have the same index, even though they cannot have the same name. The available asset types are listed in the table below:":"使用此功能，您可以从资源浏览器向任何资源添加一个或多个标签字符串。您可以提供资源名称 (作为字符串) 或其资源索引，以及单个标签字符串或每个项目是单个标签字符串的数组。如果提供资源索引值，则需要提供可选的资源类型参数 (常量)，因为不同类型的资源可以具有相同的索引，即使它们不能具有相同的名称。下表列出了可用的资源类型:",
    "{}OPTIONAL{} The type of asset to set the tags on, only used when supplying an index for the first argument.":"{}可选{}用于设置标签的资源类型，仅在为第一个参数提供索引时使用。"
}