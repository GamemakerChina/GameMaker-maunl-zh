{
    "The above code plays a sound then sets the listener(s) to play from using the mask data stored in a global variable.":"上面的代码播放声音，然后设置收听者使用存储在全局变量中的掩码数据来播放。",
    "The bitmask data to set for the sound":"为声音设置的位掩码数据",
    "The unique ID of the sound to set the mask of":"声音的唯一ID，用于设置的掩码",
    "This function can be used to set the the bit-mask for a sound so that it will play only from those listeners specified. You can create a bit-mask by using the {}{}audio_get_listener_info(){}{} and then using the bitwise {}or{} (\"|\") to create a custom mask with those listeners that you require the sound to play from, and then apply this custom mask to the sound after you have played it. This mask will over-ride the default mask or that which you may have set using the function {}{}audio_set_listener_mask(){}{}.":"此函数可用于设置声音的位掩码，以便仅从指定的收听者播放。可以使用{}{}audio_get_listener_info(){}{}创建位掩码，然后使用 bitwise{}或{}(\"|\")与需要播放声音的收听者创建自定义掩码，然后在播放声音后将此自定义掩码应用于声音。此掩码将覆盖默认掩码或使用函数{}{}audio_set_listener_mask(){}{}设置的掩码。",
    "This function can only be used on a sound after it has been set to play and requires the sound ID as returned by the function used to play the sound.":"此函数只能在声音设置为播放后使用，并且需要播放声音的函数返回的声音 ID。"
}