{
    "Each character you use is cached from the font into a texture page":"您使用的每个字符都会从字体缓存到纹理页面中。",
    "If you think you require a larger or a smaller texture page size than the default 1024px that {}GameMaker{} uses, you can set the value using this built in variable. If you are not using the {}font_add(){} function, then setting this will have no effect on your games performance as fonts added in the IDE are cached on the regular texture pages (which you can control from the {}Font Editor{} and the {}Game Options{} for the target platform).":"如果您认为需要比{}GameMaker{}使用的默认1024px更大或更小的纹理页面大小，则可以使用此内置变量设置值。如果您没有使用{}font_add(){}函数，然后，设置此设置不会影响您的游戏性能，因为在IDE中添加的字体会缓存在常规纹理页面上 (您可以从{}字体编辑器{}和目标平台的{}游戏选项{}进行控制)。",
    "The above code sets the font cache texture page to a maximum size of 512px and then adds a new font to the game.":"上面的代码将字体缓存纹理页面设置为最大512px，然后将新字体添加到游戏中。",
    "This {}built-in variable{} can be used to either get or set the texture page size when using the function {}{}font_add(){}{}. On adding a font using that function, {}GameMaker{} will create a texture page cache of the required glyphs up to the size defined by this variable. The process is as follows:":"这个{}内置变量{}可以用于在使用函数{}{}font_add(){}{}时获取或设置纹理页面大小。在使用该函数添加字体时 ，{}GameMaker{}将创建所需字形的纹理页面缓存，其大小达到此变量定义的大小。流程如下:",
    "When the texture page is full (because you use big characters or a lot of different characters) {}GameMaker{} will remove characters from the cache and replace them with new ones you need":"当纹理页面已满时(因为您使用了大字符或许多不同的字符)，{}GameMaker{}将从缓存中删除字符，并用您需要的新字符替换它们",
    "{}GameMaker{} tries not to make the texture too big because not all devices can cope with large textures, and it tries not to make the texture too small because removing and re-adding characters to the texture takes CPU time and causes your game to run slowly":"{}GameMaker{}尝试不使纹理太大，因为不是所有的设备都可以处理大的纹理，并且它尝试不使纹理太小，因为删除和重新添加字符到纹理会占用CPU时间并导致游戏运行缓慢"
}