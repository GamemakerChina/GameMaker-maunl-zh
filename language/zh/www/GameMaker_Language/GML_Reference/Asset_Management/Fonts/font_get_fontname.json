{
    "Index of the font to check.":"要检查的字体的索引。",
    "The above code will get the system name of a font resource and store it as a string in the variable \"fnt_Name\".":"上述代码将获取字体资源的系统名称，并将其存储为变量\"fnt_Name\"中的字符串。",
    "The behaviour described above only applies to font assets added through the IDE. When used on a font loaded at runtime (using {}{}font_add(){}{}), the function will return the full path to that font file instead of its font name.":"上述行为仅适用于通过IDE添加的字体资源。当在运行时加载的字体上使用时(使用{}{}font_add(){}{})，该函数将返回该字体文件的完整路径而不是其字体名称。",
    "With this function you can get the actual system name of the given font asset. This function returns a {}string{} and not an {}index{}, and the name returned will depend on the font being used. For example, you may have a font asset called \"{}fnt_Main{}\" in the Asset Browser, and the font itself may be the Windows system font {}Arial{}. In this case the function will return {}\"Arial\"{} as that is the system name of the font. If you need the name that appears in the Asset Browser, use {}{}font_get_name(){}{}.":"使用该功能，可以获取给定字体资源的实际系统名称。此函数返回{}字符串{}而不是{}索引{}，返回的名称将取决于所使用的字体。例如，您可能在资源浏览器中有一个名为\"{}fnt_Main{}\"的字体资源，字体本身可能是Windows系统字体{}Arial{}。 在这种情况下，函数将返回{}\" Arial\"{}，因为这是字体的系统名称。如果需要资源浏览器中显示的名称，请使用{}{}font_get_name(){}{}。"
}