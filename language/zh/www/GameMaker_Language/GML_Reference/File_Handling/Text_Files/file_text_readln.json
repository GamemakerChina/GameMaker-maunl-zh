{
    "The above code opens a file for reading and then loops through the lines of the file reading alternately a real number value and a string into two different arrays for future use. The file is then closed when the loop has finished.":"上面的代码打开一个用于读取的文件，然后循环通过文件的行，交替地读取一个实数值和一个字符串到两个不同的数组以供将来使用。然后，循环结束时关闭该文件。",
    "The id of the file to read from.":"要从中读取的文件的 id。",
    "With this function you can skip the remainder of the current line from a given opened text file and move to the start of the next one. The function will also return the full line as a string, making it an easy way to read complete \"chunks\" of data for parsing later.":"使用这个函数，您可以跳过当前行的其余部分，从一个给定的打开的文本文件，并移动到下一个文件的开始。该函数还将以字符串形式返回完整的行，这样就可以很容易地读取完整的“块”数据，以便稍后进行解析。"
}