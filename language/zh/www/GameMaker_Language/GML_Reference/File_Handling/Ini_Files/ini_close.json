{
    "The function will also return a string with the ini file encoded into it. This string can then be saved to a server and/or used again along with the function {}{}ini_open_from_string(){}{}&nbsp;to re-create the ini.":"该函数还将返回一个字符串，其中包含已编码的 ini 文件。 然后可以将该字符串保存到服务器和 / 或与函数 {}{}ini_open_from_string(){}{}&nbsp; 一起再次使用以重新创建 ini。",
    "This function should be called the moment you are finished reading or writing to any open ini file. If you do not use the function after you have used any of the ini write functions, then nothing will be written to disk, as the file information is held in memory until this function is called, which forces the write. If you try to open an ini without having previously closed another one (or the same one) you will get an error too.":"当您完成读取或写入任何打开的 ini 文件时，应该调用此函数。 如果在使用任何 ini 写入函数后不使用该函数，则不会将任何内容写入磁盘，因为文件信息一直保存在内存中，直到调用此函数，这会强制写入。 如果您尝试打开一个 ini 而之前没有关闭另一个 (或同一个)，您也会收到错误消息。",
    "This will open \"savedata.ini\" and read a value from the section \"save1\" with the key \"score\", then close the ini&nbsp;file. If there is no value under \"save1\" called \"score\" or there is no \"savedata.ini\" file present, 0 will be returned and a new ini file created.":"这将打开“savedata.ini”并使用键“score”从“save1”部分读取一个值，然后关闭 ini 文件。 如果“save1”下没有名为“score”的值，或者不存在“savedata.ini”文件，将返回 0 并创建一个新的 ini 文件。"
}