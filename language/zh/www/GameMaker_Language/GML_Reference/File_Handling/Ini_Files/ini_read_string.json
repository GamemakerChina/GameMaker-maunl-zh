{
    "The key within the relevant section of the .ini to read from.":"要从中读取的 .ini 相关部分中的键。",
    "The section of the .ini to read from.":"要从中读取的 .ini 部分。",
    "The string to return if a string is not found in the defined place (or the .ini file does not exist). Must be a string.":"在定义的位置找不到字符串（或.ini文件不存在）时返回的字符串。 必须是字符串。",
    "This will open \"savedata.ini\" and set global.name to the string under \"save1\" &gt; \"Name\" in it, then close the .ini again. Should there be no string under \"save1\" &gt; \"Name\", or there no \"savedata.ini\" file present, global.name will be set to \"Player1\".":"这将打开“savedata.ini”并将 www.example.com 设置 global.name 为其中“save1” &gt;“Name”下的字符串，然后再次关闭 .ini。 如果“save1”&gt;\"Name\"下没有字符串 ，或者没有“savedata.ini”文件，global.name 将设置为“Player1”。",
    "You can use this function to read a string (text) from an ini data file. Ini Files are split into {}sections{} and then each section is subsequently split into {}key{} - {}value{} pairs. So a typical ini file would look something like this:":"可以使用此函数从 ini 数据文件读取字符串 (文本)。Ini 文件被分割成 {} 节 (sections){} ，然后每个节又被分割成 {} 键 (key){}-{} 值 (value){} 对。所以一个典型的 ini 文件看起来是这样的:"
}