{
    "The key within the relevant section of the .ini to write to.":"要写入的.ini 的相关部分中的键。",
    "The real value to write to the relevant destination.":"写入相关目的地的真正值。",
    "The section of the .ini to write to.":"要写入的.ini 节。",
    "This will set score to 1000, then open \"savedata.ini\" and write this value to \"save1\" &gt; \"Score\". Then, it will set score2 to the real value under \"save1\" &gt; \"score\" in it, 1000, and close the .ini\n    again. Had it not found this value, score2 would have ended up set as 0.":"这将设置score为1000，然后打开“savedata.ini”，并将该值写入“save1”>“score”。然后，它将把score2设置为其中“save1”>“score”下的实数值1000，并再次关闭.ini。如果它没有找到这个值，score2将最终设置为0。",
    "You can use this function to write a value (numeric) to an ini data file. Ini Files are split into {}sections{} and then each section is subsequently split into {}key{} - {}value{} pairs. So a typical ini file would look something like this:":"您可以使用此函数将值（数字）写入 ini 数据文件。 Ini 文件被分成{}部分{}，然后每个部分随后被分成{}键{} - {}值{}对。 所以一个典型的 ini 文件看起来像这样："
}