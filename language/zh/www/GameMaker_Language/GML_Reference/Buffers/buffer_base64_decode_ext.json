{
    "The above code will create a buffer and store it in the variable {}buff{}, then open an INI file and read a string from it into the local variable {}_str{}. This string is then decoded into the newly created buffer before closing the INI file again.":"上述代码将创建一个缓冲区并将其存储在变量 {}buff{} 中，然后打开一个 INI 文件并将其中的字符串读取到本地变量 {}_str{} 中。然后，在再次关闭 INI 文件之前，将该字符串解码到新创建的缓冲区中。",
    "The above code will create a buffer and store the unique id for it in the variable \"buff\", then open an ini file and read a string from it into the local variable \"str\". This string is then decoded into the newly created buffer before closing the ini file again.":"上面的代码将创建一个缓冲区，并将其唯一的id存储在变量 \"buff\" 中，然后打开一个ini文件，并从中读取一个字符串到本地变量 \"str\" 中。然后将此字符串解码到新创建的缓冲区中再次关闭ini文件。",
    "The base64 encoded string to decode.":"要解码的base64编码字符串。",
    "The buffer to decode the string into.":"将字符串解码到的缓冲区。",
    "The data offset value.":"数据偏移值。",
    "The index of the buffer to decode the string into.":"将字符串解码为的缓冲区的索引。",
    "This function decodes a base64 encoded string (created using the&nbsp;{}{}buffer_base64_encode{}{} function) into a buffer.":"此函数将 Base64 编码的字符串 (使用 {}{}buffer_base64_encode{}{} 函数创建) 解码到缓冲区中。",
    "Unlike the function {}{}buffer_base64_decode{}{}, this will {}not{} create a buffer for you, but rather you should already have created the buffer (see {}{}buffer_create{}{}), which you would then use with this function. The \"offset\" is the position within the buffer to decode the given string (in bytes).":"与函数 {}{}buffer_base64_decode{}{} 不同，这 {} 不会 {} 为您创建缓冲区，而是您应该已经创建了缓冲区 (请参阅 {}{}buffer_create{}{})，您可以在然后将与此功能一起使用。 \"偏移量 \" 是缓冲区内解码给定字符串的位置 (以字节为单位)。",
    "With this function you can decode a base64 encoded string (created using the {}{}buffer_base64_encode(){}{} function) into a buffer. Unlike the function {}{}buffer_base64_decode(){}{}, this will {}not{} create a buffer for you, but rather you should already have created the buffer (see {}{}buffer_create(){}{}), the id of which you would then use with this function. The \"offset\" is the position within the buffer to decode the given string (in bytes).":"使用此函数，您可以将base64编码的字符串 (使用{}{}buffer_base64_encode(){}{}函数创建) 解码到缓冲区中。与函数{}{}buffer_base64_decode(){}{},这将{}不是{}为您创建缓冲区，而是您应该已经创建了缓冲区 (请参阅{}{}buffer_create(){}{})，然后将其id用于此函数。\"偏移量\" 是缓冲区内的位置来解码给定的字符串 (以字节为单位)。"
}