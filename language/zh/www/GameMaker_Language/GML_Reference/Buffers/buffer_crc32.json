{
    "The above code will create a CRC32 checksum hash value for the full data stored in the buffer indexed by the variable {}buff{}, and store the returned integer hash value in the variable {}check_val{}.":"上述代码将为变量 {}buff{} 索引的缓冲区中存储的完整数据创建 CRC32 校验和哈希值，并将返回的整数哈希值存储在变量 {}check_val{} 中。",
    "The above code will create a crc32 checksum hash value for the full data stored in the buffer indexed by the variable \"buff\", and store the returned integer hash value in the variable \"check_val\".":"上述代码将为变量\" Buff \"索引的缓冲区中存储的完整数据创建CRC32校验和哈希值，并将返回的整数哈希值存储在变量\"check_val \"中。",
    "The buffer to use.":"要使用的缓冲区。",
    "The data offset value.":"数据偏移值。",
    "The index of the buffer to use.":"要使用的缓冲区的索引。",
    "The size of the buffer.":"缓冲区的大小。",
    "This function will take input data from a buffer and returns a CRC32 checksum hash.":"该函数将从缓冲区获取输入数据并返回 CRC32 校验和哈希值。",
    "This function will take input data from a buffer and returns a crc32 checksum hash. You specify the buffer ID of the buffer to use, then an offset value (in bytes) for where to begin, and then a size (again in bytes) for the region to be hashed, and the function will return a 32 bit integer value for that region.":"此函数将从缓冲区获取输入数据，并返回CRC32校验和哈希。指定要使用的缓冲区的缓冲区ID，然后指定从何处开始的偏移值(以字节为单位)，然后指定要进行哈希处理的区域的大小(同样以字节为单位)，该函数将返回该区域32位整数值。",
    "You specify the buffer to use, then an offset value (in bytes) for where to begin, and then a size (again in bytes) for the region to be hashed, and the function will return a 32 bit integer value for that region.":"您指定要使用的缓冲区，然后指定开始位置的偏移值 (以字节为单位)，然后指定要散列的区域的大小 (同样以字节为单位)，该函数将为该区域返回一个 32 位整数值。"
}