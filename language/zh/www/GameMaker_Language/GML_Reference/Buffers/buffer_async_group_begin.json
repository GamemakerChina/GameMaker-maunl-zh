{
    "Note that for the console platforms (like PS4 for example), the {}groupname{}&nbsp;will be used as the save slot description, and using this function can help you avoid having the UI show for every file that is being saved out.":"请注意，对于控制台平台 (例如PS4)，{}groupname{}将用作保存插槽描述，并且使用此函数可以帮助您避免为正在保存的每个文件显示UI。",
    "The above code starts a buffer group then sets it to save out 4 files asynchronously. The group definition is then ended (at which point saving will begin).":"上面的代码启动一个缓冲区组，然后将其设置为异步保存4个文件。然后结束组定义 (此时将开始保存)。",
    "The name of the group (as a string).":"组的名称 (作为字符串)。",
    "This function is called when you want to begin the saving out of multiple buffers to multiple files (or loading multiple buffers). The {}groupname{}&nbsp;is a string and will be used as the directory name for where the files are saved, and should be used as part of the file path when loading the files back into the IDE later (using any of the {}{}buffer_load(){}{} functions). This function is {}only{} for use with the {}{}buffer_save_async(){}{}&nbsp;and {}{}buffer_load_async(){}{}&nbsp;functions and you must also end the async group by calling {}{}buffer_async_group_end(){}{} function otherwise the files will not be saved/loaded.":"当您要开始将多个缓冲区保存到多个文件 (或加载多个缓冲区) 时，将调用此函数。{}groupname{}是一个字符串，将用作文件保存位置的目录名称，并且应在稍后将文件加载回IDE时用作文件路径的一部分 (使用{}{}buffer_load(){}{}中的任何一个函数)。此函数仅{}用于{}{}{}buffer_save_async(){}{}和{}{}buffer_load_async(){}{}函数，并且您还必须通过调用{}{}buffer_async_group_end(){}{}函数来结束异步组，否则将无法保存/加载文件。"
}