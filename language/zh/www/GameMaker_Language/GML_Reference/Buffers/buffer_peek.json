{
    "If the function succeeds then a value (Real, string, boolean, etc...) will be returned depending on the data type, while a failure will return simply 0.":"如果函数成功，将根据数据类型返回一个值(实数、字符串、布尔值等)，而失败将仅返回0。",
    "The above code will get three values from three different positions within the buffer indexed in the variable \"buff\" and then use those values to set the image blend of the instance.":"上述代码将从变量\"buffe\"中索引的缓冲区内的三个不同位置获得三个值，然后使用这些值设置实例的图像混合。",
    "The index of the buffer to use.":"要使用的缓冲区的索引。",
    "The offset position (in bytes) within the buffer to read the given data from.":"缓冲区内用于读取给定数据的偏移位置(以字节为单位)。",
    "The type of data that is to be read from the buffer (see the list of constants {}here{}).":"要从缓冲区读取的数据类型(请参阅{}此处{}的常量列表)。",
    "With the {}{}buffer_read(){}{} function, you can read data from the given buffer at the current \"seek\" position, with each piece of data being read advancing this position by the bytes being read or written. However, it may be necessary for you to read a given piece of data without wanting to change the current seek position, and that\u0027s when you would use this function. You simply supply the function with a buffer id, and then an offset position (from the buffer start) within that buffer to read from, as well as the data type that you are wanting to read.":"使用{}{}buffer_read(){}{}函数，您可以在当前\"查找\"位置从给定缓冲区读取数据，读取的每个数据片段将此位置向前移动读取或写入的字节。但是，您可能需要读取给定的数据，而不希望更改当前的寻道位置，这就是您使用此函数的时候。您只需为函数提供一个缓冲区ID，然后在该缓冲区内提供一个偏移位置(从缓冲区开始)以供读取，并提供要读取的数据类型。",
    "{}NOTE{}: Using the incorrect data type for the data being read will result in erroneous values!":"{}注意{}：对正在读取的数据使用不正确的数据类型将导致错误的值！",
    "{}{}Real{}{}, {}{}Boolean{}{},&nbsp;{}{}String{}{}&nbsp;or 0 (if it fails)":"{}{}Real{}{}、{}{}Boolean{}{}、{}{}String{}{}或 0(如果失败了)"
}