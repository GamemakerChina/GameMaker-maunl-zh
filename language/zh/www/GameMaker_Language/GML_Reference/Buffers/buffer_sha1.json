{
    "In cryptography, SHA-1 is a cryptographic {}hashing{} function designed by the United States National Security Agency and is employed in several widely used applications and protocols like the popular {}Git{} where it is used to check for file changes. This function will take input data from a buffer and returns a 160 bit message digest in ASCII format. In this way you can generate a secure key which can be stored and used to check the integrity of the information being sent to (or received from) an external server (for example). When applying this to buffers using this function you must specify the buffer id of the buffer to use, then an offset value (in bytes) for where to begin, and then a size (again in bytes) for the region to be hashed.":"在密码学中，Sha-1是由美国国家安全局设计的加密{}哈希{}函数，并在几个广泛使用的应用程序和协议中使用，如流行的{}Git{}，用于检查文件更改。此函数将从缓冲区获取输入数据，并以ASCII格式返回160位消息摘要。通过这种方式，您可以生成一个安全密钥，该密钥可以存储并用于检查完整性。发送到外部服务器(或从外部服务器接收)的信息(例如)。使用此函数将其应用于缓冲区时，必须指定要使用的缓冲区的缓冲区ID，然后指定从何处开始的偏移值(以字节为单位)，然后指定要散列的区域的大小(同样以字节为单位)。",
    "The above code will create a sha1 hash for the full data stored in the buffer indexed by the variable \"buff\", and store the returned hash in the variable \"check_string\".":"上述代码将为存储在由变量\"buff\"索引的缓冲区中的完整数据创建一个sha1哈希，并将返回的哈希存储在变量\"check_string\"中。",
    "The data offset value.":"数据偏移值。",
    "The index of the buffer to use.":"要使用的缓冲区的索引。",
    "The size of the buffer.":"缓冲区的大小。",
    "{}NOTE{}: SHA-1 is not completely secure and can be broken. See&nbsp;{}this page{}&nbsp;for more info.":"{}注意{}：Sha-1并不完全安全，可能会被破坏。有关详细信息，请参阅{}本页{}。"
}