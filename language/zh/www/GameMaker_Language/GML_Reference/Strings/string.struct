{
["The above code uses the {}string{} function to draw both real numbers and strings together (draw will only accept {}either{} a string {}or{} a real, but not both)."]={old="The above code uses the {}string{} function to draw both real numbers and strings together (draw will only accept {}either{} a string {}or{} a real, but not both).";new="上面的代码使用 {} string{} 函数将实数和字符串都画在一起 (draw将只接受 {} 一个字符串 {} 或 {} 一个实数，但不接受两者)。"};
["With this function you can turn any real number into a string. If the real number is an integer, it will be saved with no decimal places, otherwise, it will be saved with two decimal places. If you require more decimal places, then use the function {}{}string_format(){}{}. Also note that using this function on a variable storing&nbsp;an {}array{}, a {}data structure{}, or a {}struct{} will convert the contents of these variables into a string which can then be output to the console or saved to a file for debugging."]={new="有了这个函数，你可以把任何实数变成一个字符串。如果实数是整数，则将其保存为无小数位，否则，将保存为两位小数。如果您需要更多的小数位，那么使用函数 {}{} string_format(){}{}。还要注意，在变量上使用此函数存储 {} 数组 {} 、 {} 数据结构 {} 或 {} 结构 {} 会将这些变量的内容转换为字符串，然后可以输出到控制台或保存到文件中进行调试。";old="With this function you can turn any real number into a string. If the real number is an integer, it will be saved with no decimal places, otherwise, it will be saved with two decimal places. If you require more decimal places, then use the function {}{}string_format(){}{}. Also note that using this function on a variable storing&nbsp;an {}array{}, a {}data structure{}, or a {}struct{} will convert the contents of these variables into a string which can then be output to the console or saved to a file for debugging."};
["The real number to be turned into a string."]={new="要变成字符串的实数。";old="The real number to be turned into a string."}
}