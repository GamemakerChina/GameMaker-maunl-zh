{
["The string to get the digits from."]={old="The string to get the digits from.";new="从中获取数字的字符串。"};
['You can use this function to parse a given string and get any numbers from it. For example, say you have this text - "I am 81 years old". With this function you would get a return string of "81".']={new="您可以使用此函数解析给定的字符串并从中获取任何数字。例如，假设您有此文本-“我已经81岁了”。有了这个函数，你会得到一个返回字符串 “81”。";old='You can use this function to parse a given string and get any numbers from it. For example, say you have this text - "I am 81 years old". With this function you would get a return string of "81".'};
['The above code will take the input string, strip it of all characters other than numbers and then set the variable {}age{} to hold the real number value of the return string (so, if the input string was - for example - "I am 18", the\n    function would return "18").']={new="上面的代码将采用输入字符串，将其除数字以外的所有字符剥离，然后将变量 {} age{} 设置为保存返回字符串的实数值 (因此，如果输入字符串为-例如-“I am 18”，则
函数将返回 “18”)。";old='The above code will take the input string, strip it of all characters other than numbers and then set the variable {}age{} to hold the real number value of the return string (so, if the input string was - for example - "I am 18", the\n    function would return "18").'}
}