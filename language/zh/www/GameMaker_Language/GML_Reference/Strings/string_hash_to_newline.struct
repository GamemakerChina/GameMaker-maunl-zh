{
["The above code converts the string with the hash symbol into a string split over two lines then prints it to the screen."]={new="上面的代码将带有哈希符号的字符串转换为拆分为两行的字符串，然后将其打印到屏幕上。";old="The above code converts the string with the hash symbol into a string split over two lines then prints it to the screen."};
["{}IMPORTANT!{} This function is provided for import compatibility between {}GameMaker{} and previous versions of the software and as such this function should not be used except when necessary to replicate obsolete functionality. Instead
    you should be using the {}escape characters{} to mark a new line. For more information on import compatibility see {}here{}."]={new="{} 重要!{} 提供此功能是为了在 {} GameMaker{} 与软件的先前版本之间导入兼容性，因此，除非有必要复制过时的功能，否则不应使用此功能。相反
您应该使用 {} 转义字符 {} 来标记新行。有关导入兼容性的更多信息，请参见 {}此处 {}。";old="{}IMPORTANT!{} This function is provided for import compatibility between {}GameMaker{} and previous versions of the software and as such this function should not be used except when necessary to replicate obsolete functionality. Instead
    you should be using the {}escape characters{} to mark a new line. For more information on import compatibility see {}here{}."};
["The string to convert over multiple lines"]={old="The string to convert over multiple lines";new="要在多行上转换的字符串"};
['This function returns a string where the "#" symbol has been converted into a new line. So a string formatted as:']={new="此函数返回一个字符串，其中 “#” 符号已转换为新行。所以一个字符串格式化为:";old='This function returns a string where the "#" symbol has been converted into a new line. So a string formatted as:'}
}