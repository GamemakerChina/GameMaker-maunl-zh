{
    "The above code checks the DS stack indexed in the variable \"move_stack\" to see if it is empty, and if it is not, it then pops the top two values from the stack and use them to set a direction for movement.":"上述代码检查变量\"move_stack\"中索引的Ds堆栈以查看其是否为空，如果不为空，则从堆栈中弹出前两个值，并使用它们来设置移动方向。",
    "The id of the data structure to pop from.":"要弹出的数据结构的ID。",
    "This function will {}pop{} the top value off of the DS stack, removing it from the stack and returning the value to be stored in a variable. If the stack is empty then the function will return the constant {}undefined{}, otherwise it will return the real or string value contained in the stack.":"此函数将{}弹出{}Ds堆栈的顶部值，将其从堆栈中删除并返回要存储在变量中的值。如果堆栈为空，则函数将返回{}未定义的常量{}，否则将返回堆栈中包含的实数或字符串值。"
}