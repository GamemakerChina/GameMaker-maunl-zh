{
    "The above code will retrieve the texture page IDs for the texture group \"MainMenu\", then check to see if they are unpacked, and if they are them they are placed into VRAM.":"以上代码将检索纹理组\"MainMenu\"的纹理页面ID，然后检查它们是否已解包，如果是，则将它们放置到 VRAM 中。",
    "The name of the texture group to check (a string)":"要检查的纹理组的名称(字符串)",
    "This function can be used to retrieve the {}texture page IDs{} of the individual pages that make up a texture group. You supply the texture group ID string (as defined in the Texture Group Editor), and the function will return a 1D array, where each entry in the array is a single texture page ID. If the function fails - ie: an invalid group is given, or the group has no texture assigned to it - then the array will be empty (0 length).":"此函数可用于检索构成纹理组的各个页面的{}纹理页ID{}。提供纹理组Id字符串(如\"纹理组编辑器\"(Texture Group Editor)中所定义)，该函数将返回一个1D数组，其中数组中的每个条目都是单个纹理页面ID。如果函数失败，即给定了无效的组，或者该组没有指定纹理，则数组将为空(长度为0)。"
}