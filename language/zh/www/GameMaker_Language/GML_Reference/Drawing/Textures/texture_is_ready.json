{
    "The above code will retrieve the texture page IDs for the texture group \"MainMenu\", then check to see if they are unpacked, and if they are not thenthey are prefetched into VRAM.":"以上代码将检索纹理组\"MainMenu\"的纹理页面ID，然后检查它们是否已解压缩，如果未解压缩，则将它们预取到VRAM中。",
    "The above code will retrieve the texture page IDs for the texture group \"MainMenu\", then check to see if they are unpacked, and if they are not they are prefetched into {}VRAM{}.":"以上代码将检索纹理组 \"MainMenu\" 的纹理页面 ID，然后检查它们是否已解压缩，如果未解压缩，则将它们预取到 {}VRAM{} 中。",
    "The texture page pointer&nbsp;{}or{} a texture group name (a string)":"纹理页面指针{}或{}纹理组名称(字符串)",
    "This function can be used to check if a specific texture page has been unpacked and is ready for use, or if a group of texture pages have been unpacked and are ready for use.":"该函数可用于检查特定纹理页面是否已解包并可供使用，或者一组纹理页面是否已解包并可供使用。",
    "This function can be used to check if a specific texture page has been unpacked and is ready for use, or if a group of texture pages have been unpacked and are ready for use. You supply the unique {}texture page ID{} (as returned by the {}texturegroup_*{} functions) or the texture group ID string (as defined in the Texture Group Editor), and the function will return {}true{} if they have been unpacked, or {}false{} otherwise.":"此函数可用于检查特定纹理页面是否已解压缩并可供使用，或者一组纹理页面是否已解压缩并可供使用。提供唯一的{}纹理页面Id{}(由{}texturegroup_*{}函数返回)或纹理组Id字符串(在纹理组编辑器中定义)，如果它们已解压缩，则该函数将返回{}true{}，否则将返回{}false{}。",
    "You supply the unique {}texture page ID{} (as returned by the {}texturegroup_*{} functions) or the texture group ID string (as defined in the {}Texture Group Editor{}), and the function will return {}true{} if they have been unpacked, or {}false{} otherwise.":"您提供唯一的 {} 纹理页面 ID{}( 由 {}texturegroup_*{} 函数返回) 或纹理组 ID 字符串 (如 {} 纹理组编辑器 {} 中定义)，如果它们已被解包，该函数将返回 {}true{}，否则返回 {}false{}。"
}