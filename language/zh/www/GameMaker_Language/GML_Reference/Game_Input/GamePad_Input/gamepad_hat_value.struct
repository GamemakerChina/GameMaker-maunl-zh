{
['The above code stores the state of the hat "0" in a local variable, then checks to see what the return value is and acts accordingly.']={new="上面的代码将帽子 “0” 的状态存储在局部变量中，然后检查返回值是什么并采取相应的行动。";old='The above code stores the state of the hat "0" in a local variable, then checks to see what the return value is and acts accordingly.'};
["Which gamepad hat (or hats) to check for."]={old="Which gamepad hat (or hats) to check for.";new="要检查哪顶游戏手柄帽子(或帽子)。"};
["Note that these can be combined (for example, supplying a hat index of 3 would be checking up and right, or you can use the {}|{} operator to combine the various values) but only at most 2 bits can be pressed at once. The function will return a real value between 0 and 1, where 0 is not pressed and 1 is fully pressed (and there may be values in between depending on whether the gamepad supports analog input for hats)."]={old="Note that these can be combined (for example, supplying a hat index of 3 would be checking up and right, or you can use the {}|{} operator to combine the various values) but only at most 2 bits can be pressed at once. The function will return a real value between 0 and 1, where 0 is not pressed and 1 is fully pressed (and there may be values in between depending on whether the gamepad supports analog input for hats).";new="请注意，这些值可以合并(例如，提供 3 的帽子索引就是向上和向右检查，或者你可以使用{}|{}运算符合并各种值)，但一次最多只能按下 2 位。该函数将返回介于 0 和 1 之间的实数值，其中不按下 0，完全按下 1(根据游戏手柄是否支持帽子的模拟输入，介于两者之间可能会有值)。"};
['Which gamepad device "slot" to check.']={new="要检查哪个游戏手柄设备 “插槽”。";old='Which gamepad device "slot" to check.'};
['With this function you can get the current value of a gamepad "hat". Each hat value is a bit-mask for the different directions where:']={new="使用此函数，您可以获得游戏手柄 “帽子” 的当前值。每个帽子值都是不同方向的位掩码，其中：";old='With this function you can get the current value of a gamepad "hat". Each hat value is a bit-mask for the different directions where:'}
}