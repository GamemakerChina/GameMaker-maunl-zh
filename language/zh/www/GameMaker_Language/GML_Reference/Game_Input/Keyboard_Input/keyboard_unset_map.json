{
    "The above example code will reset all mapped keys to their default settings if the user presses the \"escape\" key.":"如果用户按下 “escape” 键，上面的示例代码会将所有映射的键重置为其默认设置。",
    "With this function you can clear all re-mapped keys so that they return to their default state, ie: all keys to map to themselves.":"有了这个函数，您可以清除所有重新映射的键，以便它们返回到默认状态，即: 所有要映射到自己的键。"
}