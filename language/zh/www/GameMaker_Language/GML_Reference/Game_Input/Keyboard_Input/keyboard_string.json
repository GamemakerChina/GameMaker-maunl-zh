{
    "The above code will limit the length of the keyboard string to 15 characters, removing those that are over that limit by copying the first fifteen characters back into the variable.":"上面的代码将键盘字符串的长度限制为15个字符，通过将前15个字符复制回变量来删除超出该限制的字符。",
    "This variable holds a string containing the last (at most) 1024 characters typed on the keyboard. This string will only contain printable characters typed, but it {}will{} correctly respond to pressing the backspace key by erasing the last character. This variable is {}not{} read only and you can change it, for example to set it to \"\" (an empty string) if you handled it already, and you can use the {}String Functions{} to manipulate it.":"此变量保存一个字符串，该字符串包含在键盘上键入的最后 (最多) 1024字符。此字符串将仅包含键入的可打印字符，但它{}将{}通过擦除最后一个字符来正确响应按下退格键。此变量{}不是{}只读的，您可以更改它，例如，如果您已经处理过它，则将其设置为 \"\" (空字符串)，并且可以使用{}字符串函数{}来操作它。",
    "This variable holds a string containing the last (at most) 1024 characters typed on the keyboard. This string will only contain printable characters typed, but it {}will{} correctly respond to pressing the backspace key by erasing the last character. This variable is {}not{} read only and you can change it, for example to set it to \"\" (an empty string) if you handled it already, and you can use the {}String Functions{} to manipulate it. Note that when using the on-screen {}Virtual Keyboard{}, {}only{} this variable will be updated with the keyboard input.":"此变量保存一个字符串，该字符串包含在键盘上键入的最后 (最多) 1024字符。此字符串将仅包含键入的可打印字符，但它{}将{}通过擦除最后一个字符来正确响应按下退格键。此变量{}不是{}只读的，您可以更改它，例如，如果您已经处理过它，则将其设置为 “” (空字符串)，并且可以使用{}字符串函数{}来操作它。请注意，当使用屏幕上的{}虚拟键盘{}时，{}{}此变量只在用键盘输入时进行更新。",
    "{}{}NOTE{}{}&nbsp;When using the on-screen {}Virtual Keyboard{}, {}only{} this variable will be updated with the keyboard input.":"{}{}注意{}{} 当使用屏幕上的{}虚拟键盘{}时，此变量{}只{}在用键盘输入时进行更新。"
}