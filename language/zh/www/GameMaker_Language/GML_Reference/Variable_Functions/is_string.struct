{
['The above code checks the variable "val" to see if it contains a string and if it does it adds it into another string which is then assigned to the variable "name".']={new="上面的代码检查变量 “val”，看看它是否包含一个字符串，如果它这样做，它添加到另一个字符串，然后分配给变量 “名称”。";old='The above code checks the variable "val" to see if it contains a string and if it does it adds it into another string which is then assigned to the variable "name".'};
["The argument to check."]={old="The argument to check.";new="要检查的参数。"};
['This function returns whether a given variable is a string number or not. In some cases you want to check and see if a variable in {}GameMaker{} holds a string and not a real and that\'s when you would use this function. It does {}not{} return the string but rather {}true{} or {}false{}, so a value of, for example, "fish" for n will return {}true{}, but a value of 200 for n will return {}false{}.']={new="此函数返回给定变量是否为字符串号。在某些情况下，您想检查并查看{}GameMaker{}中的变量是否包含字符串而不是实数，这就是您使用此函数的时间。它不会{}返回字符串，而是{}true{}或{}false{}，因此，例如n的值为 “fish” 将返回{}true{}, 但n的值为200将返回{}false{}。";old='This function returns whether a given variable is a string number or not. In some cases you want to check and see if a variable in {}GameMaker{} holds a string and not a real and that\'s when you would use this function. It does {}not{} return the string but rather {}true{} or {}false{}, so a value of, for example, "fish" for n will return {}true{}, but a value of 200 for n will return {}false{}.'}
}