{
["This function returns whether a given variable is a boolean ({}true{} ior {}false{}) or not. In some cases you want to check and see if a variable in {}GameMaker{} holds a boolean value, and that's when you would use this function."]={new="此函数返回给定变量是否为布尔值 ({}true{}ior{}false{})。在某些情况下，您想检查并查看{}GameMaker{}中的变量是否包含布尔值，这就是您使用此函数的时间。";old="This function returns whether a given variable is a boolean ({}true{} ior {}false{}) or not. In some cases you want to check and see if a variable in {}GameMaker{} holds a boolean value, and that's when you would use this function."};
["The argument to check."]={old="The argument to check.";new="要检查的参数。"};
['The above code checks the variable "val" to see if it is a real number and if it is it then uses it to set a global variable.']={new="上面的代码检查变量 “val”，看看它是否是一个实数，如果它是，然后使用它来设置一个全局变量。";old='The above code checks the variable "val" to see if it is a real number and if it is it then uses it to set a global variable.'}
}