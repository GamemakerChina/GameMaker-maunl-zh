{
    "For {}Arrays{}, an index can be provided as the third argument that tells which index to create a reference to.":"对于{}数组{}，可以将索引作为第三个参数提供，该参数告知要创建对哪个索引的引用。",
    "More complex references can be created. For the full list&nbsp;of possibilities, see the examples.":"可以创建更复杂的引用。有关各种可能性的完整列表，请参见示例。",
    "The above code creates a reference to an array {}array{} where both the first parameter to&nbsp;{}{}ref_create{}{} (the array to reference) and the second parameter (serving as the index into the array) are themselves references. This means that if you assign a different value to the variable&nbsp;{}index{}&nbsp;later&nbsp;on, the array element indexed by&nbsp;{}ref_to_array_at_index{} also changes to the new value set. If you assign an entirely new array to the instance variable {}array{},&nbsp;{}ref_to_array_at_index{} will reference the element at the same index in that array.":"上面的代码创建了对数组 {}array{} 的引用，其中 {}{}ref_create{}{} (要引用的数组) 的第一个参数和第二个参数 (用作数组的索引) 本身都是引用。这意味着，如果稍后为变量 {}index{} 赋值不同的值，则由 {}ref_to_ray_at_index{} 索引的数组元素也会更改为新值集。如果将一个全新的数组赋给实例变量 {}array{}，则 {}ref_to_array_at_index{} 将引用该数组中相同索引处的元素。",
    "The above code first creates a struct {}the_struct{}&nbsp;in the instance executing the code that has two variables. It then creates a reference to that struct using&nbsp;{}{}ref_create{}{}&nbsp;that is then passed as the&nbsp;{}dbgrefOrStruct{}&nbsp;parameter to the next call to {}{}ref_create{}{}. This last call to&nbsp;{}{}ref_create{}{}&nbsp;creates a reference to the struct\u0027s variable&nbsp;{}a{}&nbsp;and stores it in a variable {}ref_to_struct_var{}. The struct itself is passed to the function {}indirectly{}, using the reference stored earlier in the variable&nbsp;{}ref_to_struct{}.":"上面的代码首先在执行具有两个变量的代码的实例中创建一个结构 {}the_struct{}。然后，它使用 {}{}ref_create{}{} 创建对该结构的引用，然后将该引用作为 {}dbgrefOrStruct{} 参数传递给下一次调用 {}{}ref_create{}{}。对 {}{}ref_create{}{} 的最后一次调用创建了对结构变量 {}a{} 的引用，并将其存储在变量 {}ref_to_struct_var{} 中。使用之前存储在变量 {}ref_to_struct{} 中的引用，结构本身被{20}间接地{21}传递给函数。",
    "The code above first creates an array&nbsp;{}array{} with 5 elements in the instance executing the code. It then creates a reference to index 2 (the third element) using&nbsp;{}{}ref_create{}{}. The reference is stored in a new instance variable {}ref_to_index{}.":"上面的代码首先在执行代码的实例中创建一个数组 {}array{}，该数组包含 5 个元素。然后，它使用 {}{}ref_create{}{} 创建对索引 2 (第三个元素) 的引用。该引用存储在新的实例变量 {}ref_to_index{} 中。",
    "The code above first creates an&nbsp;instance variable {}text{}&nbsp;in the Create event and then creates a&nbsp;reference&nbsp;to it using&nbsp;{}{}ref_create{}{}, that\u0027s stored in the variable {}ref_to_text{}.":"上面的代码首先在创建事件中创建一个 实例变量 {}text{} ，然后使用存储在变量 {6}ref_to_text{7} 中的 {}{}ref_create{}{} 创建对它的引用。",
    "The name&nbsp;of the variable {}as a string{}, or a reference to it":"变量的名称为{}字符串{}，或对其的引用",
    "The&nbsp;{}struct{}&nbsp;or&nbsp;{}instance{}&nbsp;containing the variable to reference, or a reference to it":"包含要引用的变量或对该变量的引用的{}结构{}或{}实例{}",
    "This function creates a reference to a {}Variable{} in a struct or instance.":"此函数用于在结构或实例中创建对{}变量{}的引用。",
    "You provide the struct or instance that the variable belongs to, {}or{} a reference to it, and the name of the variable ({}as a string{}), {}or{}&nbsp;reference to a variable that stores the name or index.":"您可以为变量所属的结构或实例、{}或{}提供对它的引用，以及变量的名称({}作为字符串{})、{}或{} 对存储名称或索引的变量的引用。",
    "{}{}NOTE{}{}&nbsp;The value can be changed through the reference in&nbsp;{}The Debug Overlay{}.":"{}{}注意{}{} 可以通过{}调试覆盖层{}中的引用更改该值。",
    "{}{}OPTIONAL{}{}&nbsp;The index in the array, if what\u0027s referenced is an&nbsp;{}array{}":"{}{}可选{}{} 数组中的索引，如果引用的是{}数组{}",
    "{}{}WARNING{}{}&nbsp;You {}cannot{} create references to {}Local Variables{}, since they exist only temporarily and cannot be referenced.":"{}{}警告{}{} 您{}无法{}创建对{}局部变量{}的引用，因为它们只是暂时存在，不能被引用。"
}