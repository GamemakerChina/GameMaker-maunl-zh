{
["The above code first creates three temporary arrays {}array1{},&nbsp;{}array2{}&nbsp;and {}array3{}. It then calls&nbsp;{}{}array_intersection{}{}&nbsp;to create a new array that is the intersection of these three arrays."]={new="上面的代码首先创建三个临时数组{}array1{}、{}array2{}和{}array3{}。 然后调用{}{}array_intersection{}{}来创建一个新数组，该数组是这三个数组的交集。";old="The above code first creates three temporary arrays {}array1{},&nbsp;{}array2{}&nbsp;and {}array3{}. It then calls&nbsp;{}{}array_intersection{}{}&nbsp;to create a new array that is the intersection of these three arrays."};
["{}{}Array{}{}&nbsp;(the intersection of the provided arrays)"]={old="{}{}Array{}{}&nbsp;(the intersection of the provided arrays)";new="{}{}数组{}{}(提供的数组的交集)"};
["{}The returned array contains the common elements with {}duplicates&nbsp;{}removed.{}"]={new="{}返回的数组包含了{}重复项{}的公共元素。{}";old="{}The returned array contains the common elements with {}duplicates&nbsp;{}removed.{}"};
["{}{}OPTIONAL{}{}&nbsp;Additional arrays to include in the intersection"]={new="{}{}可选{}{}要包含在交集中的附加数组";old="{}{}OPTIONAL{}{}&nbsp;Additional arrays to include in the intersection"};
["{}{}NOTE{}{}&nbsp;The order of elements in the new array is {}not{} ensured. If you need to sort the array you will need to do so afterwards."]={old="{}{}NOTE{}{}&nbsp;The order of elements in the new array is {}not{} ensured. If you need to sort the array you will need to do so afterwards.";new="{}{}注意{}{}新数组中元素的顺序是{}没有{}保证的。如果您需要对数组进行排序，则需要在之后进行排序。"};
["{}This function returns a {}new{} array with the {}intersection{} of all arrays that you provide to it.{}"]={new="{}此函数返回一个{}新{}数组，该数组具有您提供给它的所有数组的{}交集{}。{}";old="{}This function returns a {}new{} array with the {}intersection{} of all arrays that you provide to it.{}"};
["The first array"]={new="第一个数组";old="The first array"};
["The second array"]={old="The second array";new="第二个数组"}
}